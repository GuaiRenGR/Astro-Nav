# Astro 导航站

一个现代化、高性能的网站导航平台，提供流畅的动画体验和强大的内容组织能力。

## 特性

- ⚡ 基于 Astro 构建，极速加载
- 🎨 流畅的动画和交互体验
- 🔍 强大的搜索功能（支持拼音搜索）
- 🏷️ 灵活的标签分类系统
- 🔤 音序排序（支持中英文）
- 🖱️ 自定义光标动效
- 🌓 亮暗主题切换
- 📱 完全响应式设计

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 添加网站

编辑 `src/data/sites.ts` 文件，在 `sites` 数组中添加新网站：

```typescript
{
  name: '网站名称',
  url: 'https://example.com',
  description: '网站描述',
  icon: 'https://example.com/icon.png', // 或本地路径 /icons/example.svg
  tags: ['标签1', '标签2'],
}
```

### 图标管理

支持三种方式：

1. **在线图标**：直接使用 URL
2. **本地图标**：将图标放在 `public/icons/` 目录，使用 `/icons/example.svg`
3. **图标库**：使用 Iconify 等图标库

### 标签颜色

在 `src/data/sites.ts` 的 `tagColors` 对象中配置标签颜色：

```typescript
export const tagColors: Record<string, string> = {
  '开发工具': '#3B82F6',
  '设计资源': '#8B5CF6',
  // 添加更多...
};
```

## 技术栈

- **Astro** - 静态站点生成器
- **TypeScript** - 类型安全
- **Tailwind CSS** - 样式框架
- **Fuse.js** - 模糊搜索
- **Pinyin-Pro** - 中文拼音处理

## 项目结构

```
nav/
├── src/
│   ├── components/      # 组件
│   │   ├── SiteCard.astro
│   │   ├── SearchBar.astro
│   │   ├── TagFilter.astro
│   │   └── CustomCursor.astro
│   ├── data/           # 数据配置
│   │   └── sites.ts    # 网站配置文件
│   ├── layouts/        # 布局
│   │   └── Layout.astro
│   ├── pages/          # 页面
│   │   └── index.astro
│   ├── utils/          # 工具函数
│   │   └── siteProcessor.ts
│   └── types.ts        # 类型定义
├── public/             # 静态资源
│   └── icons/          # 图标文件
└── package.json
```

## 配置

### Tailwind CSS

配置文件：`tailwind.config.mjs`

### Astro

配置文件：`astro.config.mjs`

## 浏览器支持

- Chrome/Edge (最新版本)
- Firefox (最新版本)
- Safari (最新版本)

## License

MIT
